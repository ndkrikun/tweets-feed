/**
 * How many tweets should be loaded by default
 */
export const DEFAULT_TWEETS_QUANTITY = 10;

/**
 * Interval between each new feed update
 */
export const UPDATE_FEED_INTERVAL_PERIOD = 2 * 1000;

/**
 * Maximun twitter id. When api returns it, that means that data base is full
 */
export const MAX_TWEET_ID = 10000;
