/**
 * How many tweets should be loaded we app is started
 */
export const INITIAL_TWEETS_QUANTITY = 10;

/**
 * Interval between each new feed update
 */
export const UPDATE_FEED_INTERVAL_PERIOD = 2 * 1000;
